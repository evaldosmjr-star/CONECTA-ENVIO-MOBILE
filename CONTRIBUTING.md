# Contribuindo

Use Kotlin, Compose e Material 3. Mantenha o funcionamento local, sem backend e sem automacao direta da interface do WhatsApp.

Antes de enviar alteracoes:

```bash
./gradlew clean test assembleDebug
```

Nao inclua contatos reais, senhas, chaves privadas ou dados sensiveis no repositorio.
